# 검토 및 수정 기록

## 수정 사항

1. **다리 건너기 검증기 무한 재귀 수정**
   - 기존 `bridgeSolver.js`는 상태 순환을 재귀 탐색하면서 메모 값을 재귀 종료 후에만 기록해 호출 스택이 넘칠 수 있었습니다.
   - 유한 상태 그래프에 대한 다익스트라 탐색으로 교체하여 최소 시간 17분과 최적 경로를 안정적으로 계산합니다.

2. **힌트 사용 횟수 이중 집계 수정**
   - 힌트를 누를 때 이미 저장된 횟수가 제출 시 다시 더해지던 문제를 수정했습니다.

3. **30개의 열린 문 재시작 초기화 수정**
   - `다시 시작`을 눌러도 이전 답안 입력값이 남던 문제를 수정했습니다.

4. **자동 테스트의 학생 기록 오염 방지**
   - `test.html` 실행 전 학습 기록을 백업하고 테스트 종료 후 원래 상태로 복원하도록 수정했습니다.

5. **회귀 테스트 추가**
   - 힌트 이중 집계 방지
   - 열린 문 답안 입력 초기화

## 검증 결과

- JavaScript 문법 검사: 통과
- DOM 모의 환경 자동 테스트: **206 / 206 통과**
- 문제 수: 24개(추상화 8, 알고리즘 9, 통합 7)
- 공통 게임 엔진: 17개

브라우저에서는 `test.html`을 열어 실제 DOM·레이아웃까지 최종 확인하세요.
